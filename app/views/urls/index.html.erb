<h1>URLs</h1>

<% @urls.each do |url| %> 
    <ul>
      <li><%= link_to url.short_url, url.short_url %> - <%= url.long_url %></li>
    </ul>
  <% end %>
  
  <%= link_to 'Create another link', new_url_path %>

